database:
  dbname: splus
  user: gustavo
  password: asdflkjh
  host: 192.168.10.22
  port: 5432

general:
  injection_processes: 1
  max_tasks_per_child: 1
  # Set this to fail instead of hanging forever when no file completes.
  # Example: 3600 means abort after 1 hour without worker progress.
  injection_worker_timeout_seconds: 600
